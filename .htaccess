# ExaContable - Configuración para cPanel con Node.js
# Este archivo asume que cPanel maneja el proxy reverso hacia Node.js

# --- Seguridad ---

# Deshabilitar listado de directorios
Options -Indexes

# Proteger archivos sensibles
<FilesMatch "\.(env|env\.local|env\.production|json|lock|ts|tsbuildinfo|md)$">
  Require all denied
</FilesMatch>

<IfModule mod_rewrite.c>
  RewriteEngine On

  # Bloquear acceso a directorios internos
  RewriteRule ^\.next/(?!static/) - [F,L]
  RewriteRule ^node_modules/ - [F,L]
  RewriteRule ^whatsapp-auth/ - [F,L]
  RewriteRule ^prisma/ - [F,L]
  RewriteRule ^scripts/ - [F,L]
  RewriteRule ^lib/ - [F,L]
  RewriteRule ^store/ - [F,L]
</IfModule>

# --- Cache de assets estáticos ---
<IfModule mod_expires.c>
  ExpiresActive On

  # CSS, JS, imágenes con hash - cachear por 1 año
  <FilesMatch "\.(js|css|woff2?)$">
    ExpiresDefault "access plus 1 year"
  </FilesMatch>

  # Imágenes
  <FilesMatch "\.(png|jpg|jpeg|gif|ico|svg|webp)$">
    ExpiresDefault "access plus 1 month"
  </FilesMatch>
</IfModule>

# --- Compresión ---
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/css text/xml application/json application/javascript application/x-javascript image/svg+xml
</IfModule>
